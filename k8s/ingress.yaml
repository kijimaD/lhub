apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: apps-ingress
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: or.localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: or-server
            port:
              number: 3000
  - host: srd.localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: srd-server
            port:
              number: 8000
  - host: opv.localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: opv-server
            port:
              number: 8007
  - host: q.localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: q-server
            port:
              number: 8000
  - host: mypdfs.localhost
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mypdfs-server
            port:
              number: 8000
